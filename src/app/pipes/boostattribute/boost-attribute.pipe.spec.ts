import { BoostAttributePipe } from './boost-attribute.pipe';

describe('BoostAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new BoostAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
